# Pod Service Account

There is ServiceAccount `secret-reader` in Namespace `project-swan`. Create a
Pod of image `nginx:1-alpine` named `api-contact` which uses this
ServiceAccount.

Use `kubectl exec` to log into the Pod, then use `curl` to manually query all
Secrets from the Kubernetes API.

Write the result into file `/tmp/answers/cluster-secrets.json`.
