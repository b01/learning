# Install cert-manager

Install this after the aws-vpc-cni and AWS Cloud Controller Manager.

1. First, install `cmctl`:
   ```shell
   wget https://github.com/cert-manager/cmctl/releases/download/v2.4.1/cmctl_linux_amd64
   chmod +x cmctl_linux_amd64
   sudo mv cmctl_linux_amd64 /usr/local/bin/cmctl
   ```
2. Install `cert-manager`
   ```shell
   wget https://github.com/cert-manager/cert-manager/releases/download/v1.21.1/cert-manager.yaml

   kubectl apply -f cert-manager.yaml
   ```

Next: [Resources]

Back: [Cluster Installation Overview]

---

[Resources]: /kubernetes/006.0-cluster-install-on-aws.md#resources
[Cluster Installation Overview]: /kubernetes/002.0-cluster-installation-overview.md
