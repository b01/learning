# kube-apiserver

It handles any HTTP API request and updates the state in `etcd`.

Except for etcd, every component communicates with the kube-apiserver. Making it
a dependency for all other components. Even though the `kubelet` is responsible
for starting the `kube-apiserver`.

The Kubernetes API server validates and configures data for the api objects
which include pods, services, replicationcontrollers, and others. The API
Server services REST operations and provides the frontend to the cluster's
shared state through which all other components interact.

Next: [Cluster Component kube-controller-manager]

---

[Cluster Component kube-controller-manager]: /kubernetes/001.4-kube-controller-manager.md
