# Make A Sidecar

In the namespace `log` there is a deployment `worker` that writes to a log file
`/var/log/worker.log`.

Make a sidecar that tails the log file from another container. You are only
allowed to modify the volume mounts of the original container.
